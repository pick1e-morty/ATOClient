def getDataInExcelData_TW(self):
    # 遍历excelDataTW，把文本数据保存为一个列表

数据索引
1.创建一个数据索引，界面显示的数据及增加删减的都是这个数据源，就当这是一个list。
但这样不行，因为当你将数据与Qt控件的项关联时，实际上是将该数据的副本与项关联。如果你将一个列表与项关联，并尝试直接修改该列表，那么对列表的修改不会反映在与项关联的数据上。
如果这样做的话，就需要先从item中取出data，这个data就是list，改完之后再放回去，也没什么。但如果是上百上前的操作呢？那肯定还不如另一个方案呢

另外还有个弊端用户操作界面，删除选中项后，我是用TableWidget.selectedItems()来获取选中项的
那这个selectedItems对应到list中的那个元素呢？我要再for循环对比一边吗。直接按照最初的顺序，反正不对
